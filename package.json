{
  "name": "su-card-to-video",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal HTML/CSS card-to-video starter skill using HyperFrames, GSAP, and FFmpeg.",
  "scripts": {
    "doctor": "bash scripts/check-env.sh",
    "render": "bash scripts/render.sh"
  },
  "devDependencies": {
    "hyperframes": "latest"
  }
}

